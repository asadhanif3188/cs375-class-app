import 'package:flutter/material.dart';

class DoctorGrid extends StatelessWidget {
  const DoctorGrid({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Doctor Grid"));
  }
}
